# MongoDB connection URI
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=

# JWT Secret Key
# Used to sign and verify JWTs, should be a strong, random string
JWT_SECRET=

# Port for the Express server to listen on
# Defaults to 5000 if not set in the environment
PORT=5000